{
  "name": "consume-sourcemap",
  "version": "1.0.0",
  "description": "Consumes a sourcemap using source-map npm library",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rosbel",
  "license": "ISC",
  "dependencies": {
    "source-map": "^0.7.4"
  }
}
